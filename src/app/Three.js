import * as THREE from "three";
import { DoubleSide } from "three";

// Eporting all to use as common file.
export const scene = new THREE.Scene();
export const doubleSide = DoubleSide;
export default THREE;